import { Typography } from "@mui/material";

export const HomeButton = () => {
  return (
    <Typography
      component="a"
      variant="h6"
      sx={{
        color: "inherit",
        textDecoration: "none",
        cursor: "pointer",
      }}
    >
      paysible
    </Typography>
  );
};
